# Release notes

One file per release, named after the tag: `docs/release/v0.11.0.md`.

The file at the tag being built **is** the GitHub Release body. `release.yml` passes it to
`action-gh-release` as `body_path` on every leg, so the note is written, reviewed and
committed together with the version bump — before the tag exists — and nothing comes back
after CI to rewrite it. Each job runs `scripts/check-release-note.mjs` first, which fails
in seconds when the file is missing or empty.

Conventions:

- Written in **English**, since it is read on GitHub by an international audience. This is a
  writing guideline rather than a gate — the checker does not inspect the language, because a
  release must not be blockable by a regex over prose.
- The name is the tag, `v` prefix included. Nothing else selects the note.
- Use Markdown. The final `**Full Changelog**` compare link generated by
  `.agents/skills/release/scripts/release-notes.mjs` belongs in it.
- Do not edit a note after its tag is pushed and expect the published release to change:
  the workflow reads the committed file at the tag, so a correction is a `gh release edit`
  (and a fix to the file for the record).

The flow that writes one is `.agents/skills/release/SKILL.md`.
