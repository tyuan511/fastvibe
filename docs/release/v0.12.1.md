### Fixed

- Reduced SSH Agent runtime size by keeping the local memory and ONNX/Transformers stack out of the remote Agent dependency graph. Desktop memory features remain available locally, while remote Agents start without downloading the embedding runtime.
- Fixed project drag-and-drop ordering in the sidebar when the drag target rectangle is cleared during drop handling.

**Full Changelog**: https://github.com/tyuan511/fastvibe/compare/v0.12.0...v0.12.1
