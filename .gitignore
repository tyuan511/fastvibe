node_modules
out
dist
dist-electron
build
# electron-builder's output dir only. Anchored: a bare `release` also swallowed
# `.agents/skills/release/`, so the release skill could not be committed.
/release/
.DS_Store
*.log
*.tsbuildinfo
*.tgz
.env
.env.*
.firecrawl
*.local
.vscode
.idea
resources/models-dev/**
!resources/models-dev/.gitkeep
.firecrawl
.openchamber
